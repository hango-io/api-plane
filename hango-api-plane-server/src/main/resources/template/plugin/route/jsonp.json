{
  "inject": {
    "kind": "jsonp"
  },
  "layouts": [
    {
      "key": "callback",
      "alias": "callback",
      "type": "input",
      "help": "用于指定HTTP请求中的回调函数名，例如http://example.com/data?test=myCallback跨域请求，需要配置callback为test",
      "rules": [
        "Required",
        "MaxLength(200)"
      ]
    }
  ]
}
